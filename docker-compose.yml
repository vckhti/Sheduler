version: '3.4'
services:

  app:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: ${DOCKER_CONTAINER_NAME:api-gw-laravel}
    tty: true
    restart: unless-stopped
    ports:
      - ${DOCKER_CONTAINER_PORT:-80}:80
    volumes:
      - ./:/var/www/html/

